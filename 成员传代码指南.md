# 实验室成员传代码指南

> 给实验室成员看的操作说明。先看第 0 步，再选一种方式开始。

## 0. 前置准备（每个人都必须做）

1. 注册 GitHub 账号：https://github.com/signup （免费，记住用户名）
2. 把**用户名**发给实验室管理员
3. 管理员把你加入对应仓库的 **Collaborators**（Settings → Collaborators → Add people）
4. 你会收到 GitHub 邀请邮件，点 **Accept invitation** 接受
5. 之后访问仓库地址（如 `https://github.com/zua-robot-lab/control`）就能看到内容了

> 没被加入前，访问仓库会显示 404——这是正常的保密效果。

---

## 方式一：网页直接上传（最简单，适合传文件/文档）

1. 登录 GitHub，打开管理员发给你的仓库地址，例如 `https://github.com/zua-robot-lab/control`
2. 点页面上的 **Add file** → **Upload files**
3. 把文件直接拖进页面（或点 "choose your files" 选择）
4. 页面下方填一句说明（例如"更新底盘代码 v1.2"）
5. 点绿色按钮 **Commit changes**，完成 ✅

> 适合：单个文件、文档、配置文件。**不适合**：大型代码工程（上传不了空文件夹和大文件，整包代码请用方式二/三）。

---

## 方式二：GitHub Desktop（图形界面，推荐日常开发）

1. 下载安装：https://desktop.github.com
2. 打开后用你的 GitHub 账号**登录**
3. **File → Clone repository** → 选择实验室仓库（control / vision / hardware / mechanical）→ 选个本地保存位置 → **Clone**
4. 以后每次更新代码：
   - 在本地文件夹里修改/添加文件
   - GitHub Desktop 会自动显示改动 → 左下角填 **Summary**（这次改了什么）→ 点 **Commit to main**
   - 点右上角 **Push origin**，代码就上传到 GitHub 了
5. 看别人最新代码：点 **Fetch origin**（拉取更新）/ **Pull**

---

## 方式三：命令行（适合开发主力）

```bash
# 第一次：克隆仓库（把 control 换成你负责的方向）
git clone https://github.com/zua-robot-lab/control.git
cd control

# 日常更新
git add .
git commit -m "更新说明"
git push
```

第一次 push 会弹出 GitHub 登录窗口，登录一次后这台电脑就不用再登了。

---

## 常见问题

| 问题 | 解决 |
| --- | --- |
| 提示没有权限 / 404 | 还没被加入仓库，联系管理员加 Collaborators |
| 不知道别人改了什么 | 打开仓库网页，看 **Commits**（提交记录） |
| push 提示冲突（conflict） | 先 `git pull` 拉取最新，再改再 push |
| 想备份整个仓库 | 网页上 **Code → Download ZIP** |
| 传大文件 | 单个文件别超过 100MB；超大文件另想办法（联系管理员） |

---

*实验室网站：https://L-droid-dev.github.io/lab-website/ （项目展示 → 四个方向）*
