# 网站安全与防篡改保护说明

> 目的：防止网站崩溃、防止他人篡改网站内容（尤其是塞入违法内容导致域名/账号被封）。
> **核心原则：网站是公开的，能推代码的人 = 能控制网站内容。所以一切保护围绕「管好谁能推代码」。**

## 一、防篡改（最有效：GitHub 分支保护）

**在代码仓库启用分支保护（必做，最重要的一步）：**
仓库 `lab-website` → **Settings → Branches → Add branch protection rule**：
- Branch name pattern：`main`
- ✅ **Require a pull request before merging** + 至少 **1 人批准**
- ✅ **Do not allow force pushes**（禁止强推）
- ✅ 分支可推送限制：只允许核心负责人（Admin）

**效果**：任何人（包括有写权限的成员）都**不能直接改 main 分支上线**，改动必须先提交 Pull Request、经人审核才合并 → 从根上防止乱改和塞入非法内容。

代码仓库（control/vision/hardware/mechanical）同样设置。

## 二、权限最小化

| 角色 | 权限 | 建议人数 |
| --- | --- | --- |
| Admin（仓库管理员） | 全部权限 | 1~2 个核心负责人 |
| Write（成员） | 提交代码 / 提 PR | 按需，分方向给 |
| Read | 只读 | 老师/参观者 |

- 组织 `zua-robot-lab`：建 Teams（控制组/视觉组/硬件组/机械组/管理员组），成员进对应组，仓库权限**按组分配**
- 毕业/退出成员：及时从组织/仓库移除
- **绝不把 Admin 给闲杂人**——能强推的人越少越安全

## 三、账号安全

- 所有 Admin / Owner 开启 **两步验证（2FA）**：GitHub → Settings → Password and security
- 账号密码不共用、不外借
- 不在公共电脑保存登录状态
- 定期检查 GitHub → Settings → Security log（登录记录）

## 四、定期巡检（建议每月一次）

1. 打开线上网站 https://zua-ril-thrive.cn 快速扫一遍（有没有陌生内容/异常页面）
2. 仓库 → **Insights → Network**（提交图）看有没有异常提交/陌生提交者
3. 检查域名控制台：解析记录有没有被改过、账号有没有异常登录

## 五、防崩溃 / 恢复能力

- **git 版本管理本身就是最强备份**：每次推送都是存档点，任何坏改动可回滚
- 出问题立即恢复：
  ```bash
  git log --oneline            # 找最近正常版本
  git revert <提交号>          # 撤销坏提交（安全）
  git push                     # 1 分钟后线上恢复
  ```
- 每学期做一次**本地存档备份**（`git clone` 或打包 zip 存到实验室网盘）

## 六、内容合规红线（防违法风险）

发布到**公开网络**的内容请自查，违禁内容会导致域名被封、账号被处理：
- ❌ 涉黄赌毒、诈骗、赌博、黑产相关内容
- ❌ 侵权素材（盗版软件、未授权转载、侵权图片/字体）
- ❌ 冒用其他单位名义、虚假宣传（证书/获奖需真实）
- ❌ 诱导分享/点击的恶意外链
- ✅ 发布前多人过目（正好配合 PR 审核流程）

## 七、应急响应（发现被篡改/被挂异常内容）

1. **第一时间**：仓库 Settings → 移除可疑成员权限；必要时 **Pages → Unpublish**（先下线再查）
2. `git log` 查看异常提交的时间与提交者
3. **回滚**到最近正常版本并重新发布
4. 排查是否账号泄露：改密码、开启 2FA、**吊销所有 token**（Settings → Developer settings → Personal access tokens）
5. 若涉及违法内容被利用：向 GitHub Support（support.github.com）如实说明

## 八、操作清单（新负责人上任时照做）

- [ ] main 分支保护已开启（PR + 禁强推）
- [ ] Admin 仅 1~2 人，成员按 Teams 分权
- [ ] 全员 2FA 开启
- [ ] 维护日志已交接到手
- [ ] 本地存档备份已建立
- [ ] 了解应急回滚流程（见第七节）
